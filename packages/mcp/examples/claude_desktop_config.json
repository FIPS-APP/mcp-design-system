{
  "_comment_": "Add this entry to ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) or %APPDATA%/Claude/claude_desktop_config.json (Windows). Restart Claude Desktop afterwards.",
  "mcpServers": {
    "fips-ds": {
      "command": "npx",
      "args": ["-y", "@fips-app/mcp-design-system"]
    }
  }
}
